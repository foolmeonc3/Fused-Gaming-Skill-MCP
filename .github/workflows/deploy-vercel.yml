name: Deploy to Vercel

on:
  push:
    branches: [main, develop]
    paths:
      - 'packages/web/**'
      - '.github/workflows/deploy-vercel.yml'
  pull_request:
    branches: [main]
    paths:
      - 'packages/web/**'

jobs:
  check-secrets:
    runs-on: ubuntu-latest
    outputs:
      has-token: ${{ steps.check.outputs.has-token }}
    steps:
      - id: check
        run: |
          if [ -z "${{ secrets.VERCEL_TOKEN }}" ]; then
            echo "has-token=false" >> $GITHUB_OUTPUT
            echo "⚠️ VERCEL_TOKEN not configured. To enable deployments, add VERCEL_TOKEN to repository secrets."
          else
            echo "has-token=true" >> $GITHUB_OUTPUT
          fi

  deploy:
    needs: check-secrets
    runs-on: ubuntu-latest
    if: needs.check-secrets.outputs.has-token == 'true'
    env:
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

    steps:
      - uses: actions/checkout@v5

      - name: Use Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22.x'
          cache: 'npm'

      - name: Install Vercel CLI
        run: npm install -g vercel@latest

      - name: Install dependencies
        run: npm install --package-lock-only --ignore-scripts && npm ci

      - name: Build project
        run: npm run build --if-present

      - name: Deploy to Vercel (Production)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          cd packages/web
          vercel --prod --token=${{ secrets.VERCEL_TOKEN }} --yes
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

      - name: Deploy to Vercel (Preview)
        if: github.event_name == 'pull_request' || (github.event_name == 'push' && github.ref != 'refs/heads/main')
        run: |
          cd packages/web
          vercel --token=${{ secrets.VERCEL_TOKEN }} --yes
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

      - name: Comment PR with deployment URL
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '🚀 Vercel deployment preview is ready!\n\nVisit the deployment preview to review changes.'
            })

  skip-deploy:
    needs: check-secrets
    runs-on: ubuntu-latest
    if: needs.check-secrets.outputs.has-token != 'true'
    steps:
      - name: Notify about missing secrets
        run: |
          echo "⚠️ Deployment skipped: VERCEL_TOKEN is not configured"
          echo "To enable Vercel deployments:"
          echo "1. Create a Vercel API token at https://vercel.com/account/tokens"
          echo "2. Add it as VERCEL_TOKEN in GitHub repository secrets"
          echo "3. (Optional) Add VERCEL_ORG_ID and VERCEL_PROJECT_ID for faster deployments"
