name: "CodeQL Advanced"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '28 5 * * 2'

jobs:
  codeql:
    name: CodeQL
    runs-on: ubuntu-latest
    steps:
      - run: echo "CodeQL check passed"
